// Package goschedule is an in-process job scheduler for Go.
//
// It supports one-shot and recurring jobs with named queues, priorities, retries,
// per-job timeouts, lifecycle hooks, and a pluggable Store interface — see the
// repository README for usage examples and the available backends.
package goschedule
